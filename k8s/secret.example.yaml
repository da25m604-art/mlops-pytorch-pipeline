# placeholder only, never commit real values
# create the real secret with:
#   kubectl create secret generic ml-secrets -n ml-training \
#     --from-literal=REGISTRY_TOKEN=<real-token>
apiVersion: v1
kind: Secret
metadata:
  name: ml-secrets
  namespace: ml-training
type: Opaque
stringData:
  REGISTRY_TOKEN: "REPLACE_ME"
